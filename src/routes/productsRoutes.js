import { Router } from "express";

const router = Router()


router.get('/', );
router.get('/:id', );
router.get('/pid/:pid', );
router.post('/', );
router.put('/:pid', );
router.delete('/:pid', );

export default router;